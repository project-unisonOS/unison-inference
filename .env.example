# Unison Inference Service
# Feature flags and security controls

# Require consent grants for protected endpoints. When true, /inference/request
# enforces appropriate consent scopes (e.g., REPLAY_READ).
# Default: false (staged rollout)
UNISON_REQUIRE_CONSENT=false
